# pico-pico

## caldav-http-server
To build and run docker image run following command:
```
docker compose up -d
```
Make sure that you create appropriate `.env` file that contains a `CALDAV_SERVER_URI` definition.